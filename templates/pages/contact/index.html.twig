{% extends 'base.html.twig' %}

{% block title %}Contact - Symrecipe{% endblock %}

{% block body %}
<div class="container">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mt-4">
            {{ message }}
        </div>
    {% endfor %}
     <h1 class="mt-5">Contact form</h1>
    {{ form(contactForm) }}
</div>
{% endblock %}


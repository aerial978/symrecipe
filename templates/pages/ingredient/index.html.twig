{% extends 'base.html.twig' %}

{% block title %}Ingredient Index -Symrecipe{% endblock %}

{% block body %}

<div class="container mt-5">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mt-4">
            {{ message }}
        </div>
    {% endfor %}

    {% if not ingredients.items is same as([]) %}
    <h1>My Ingredients</h1>
    <div class="d-flex justify-content-between my-4">
        <div class="count">
            <h5>Total ingredient : {{ ingredients.getTotalItemCount }}</h5>
        </div>
         <a href="{{ path('ingredient.new') }}" class="btn btn-primary">Add</a>
    </div>

        <table class="table table-hover">
            <thead>
                <tr class="text-center">
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Price</th>
                <th scope="col">Created At</th>
                <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                {% for ingredient in ingredients %}
                    <tr class="table-primary">
                        <th scope="row">{{ ingredient.id }}</th>
                        <td>{{ ingredient.name }}</td>
                        <td>{{ ingredient.price|number_format(2, '.', ',') }} €</td>
                        <td>{{ ingredient.createdAt|date('d/m/Y') }}</td>
                        <td><a href="{{ path('ingredient.edit', {id: ingredient.id}) }}" class="btn btn-info">Edit</a></td>
                        <td><a href="{{ path('ingredient.delete', {id: ingredient.id}) }}" class="btn btn-warning">Delete</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <div class="navigation d-flex justify-content-center mt-4">
        {{ knp_pagination_render(ingredients) }}
        </div>
    {% else %}
        <h4>There is no ingredients !</h4>
        <a href="{{ path('ingredient.new') }}" class="btn btn-primary">Add</a>
    {% endif %}
</div>
{% endblock %}

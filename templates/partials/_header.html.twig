<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container py-2">
         <a href="{{ path('home.index') }}">
            <h2 class="text-light ml-5">Symrecipe</h2>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a href="{{ path('home.index') }}"><h4 class="nav-link">Home</h4></a>
                        <span class="visually-hidden">(current)</span>
                    </h4>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a href="{{ path('ingredient.index') }}"><h4 class="nav-link">Ingredient</h4></a>
                        <span class="visually-hidden">(current)</span>
                    </h4>
                </li>
                <li class="nav-item">
                    <a href="{{ path('recipe.index') }}"><h4 class="nav-link">Recipe</h4></a>
                        <span class="visually-hidden">(current)</span>
                    </h4>
                </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav flex-row ms-auto">
                {% if app.user %}
                    <div class="d-flex">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName }}</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">My profil</a>
                                <a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id}) }}">Change my password</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('security.logout') }}">Logout</a>
                            </div>
                        </li>
                    </div>
                {% else %}
                <li class="nav-item">
                    <a href="{{ path('security.login') }}"><h4 class="nav-link">Sign in</h4></a>
                        <span class="visually-hidden">(current)</span>
                    </h4>
                </li>
                <li class="nav-item">
                    <a href="{{ path('security.registration') }}"><h4 class="nav-link">Sign up</h4></a>
                        <span class="visually-hidden">(current)</span>
                    </h4>
                </li>
                {% endif %}  
            </ul>
        </div>
    </div>
</nav>